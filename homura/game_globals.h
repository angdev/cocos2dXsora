// Ŭnicode please
#pragma once

typedef enum {
  kGameCompPlayer,
  kGameCompItem,
  kGameCompBullet,
  kGameCompEnemy,
  kGameCompPhy,
  kGameCompDrawable, //일단 넣기
} kCompType;

typedef enum {

} kObjectType;
